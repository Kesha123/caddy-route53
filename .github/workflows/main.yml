name: main

on:
  workflow_dispatch:
  push:
    branches: [main]

permissions:
  packages: write

jobs:
  build:
    uses: ./.github/workflows/build.yml

  publish:
    needs: build
    uses: ./.github/workflows/publish.yml
