name: pull-request

on:
  workflow_dispatch:
  pull_request:

permissions:
  packages: write
  security-events: write

jobs:
  build:
    uses: ./.github/workflows/build.yml
    with:
      suffix: pr
